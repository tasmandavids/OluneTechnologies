# Master Update — 2026-09-01

Generated by an autonomous audit session on 2026-09-02. No `DAILY_REPORT.md` or
`NEXT_STEPS_HANDOFF.md` exists in this repo — the closest equivalents were
`OLUNE_PROGRESS.md` (build log, last updated Session 26 / 2026-06-22),
`MARKET_READINESS.md` (external audit, 2026-08-06), and the bodies of the two
open pull requests. This file is the handoff going forward.

---

## 1. Environment Status

### GitHub — mostly aligned, 2 PRs still open
- `main` is at `45ab14f` (merge of PR #49, 2026-09-01 04:53 UTC). CI (`ci.yml`)
  is green on that push: `quality` (unit tests, typecheck, lint, i18n check,
  build, prod-dependency audit) and `migrate-and-seed` both succeeded.
- **Not merged:**
  - **PR #51** (`claude/todays-plan-cvhk9j` → `main`, **draft**) — Sentry error
    monitoring + a shared Upstash-Redis rate limiter. Directly closes
    `MARKET_READINESS.md` §1.1 and §1.2, the audit's own top two items. CI green,
    Vercel preview built successfully. Left in draft, so not auto-merged here —
    needs a human to take it out of draft and merge.
  - **PR #50** (`ttest` → `main`) — adds `SECURITY.md`. CI green, Vercel preview
    built successfully. The PR body flags a real pre-merge blocker: confirm
    `security@olune.co.nz` actually receives mail (MX records) before merging,
    or swap in `support@olune.co.nz`. Not verified this session (no mail access).
- **Stale, already-merged branches** (safe cleanup candidates, not deleted here):
  `soc2-merge` and `claude/soc2-compliance-exam-nd6akr` — both are ancestors of
  `main` (content landed via PR #48). No unique commits remain on either.
- No merge conflicts found anywhere in the current branch set.

### Supabase — in sync as of the last main push
- Migration frontier on `main`: **0126** (`0126_audit_events.sql`), added
  yesterday alongside the SOC2 remediation merge.
- `.github/workflows/ci.yml`'s `migrate-and-seed` job runs on every push to
  `main` and, on the 2026-09-01 04:57 UTC run (PR #49 merge), its steps —
  *Push migrations*, *Show migration status*, *Verify migration parity*,
  *Verify function grants* — all completed successfully against project
  `wnoxcwihrzbxvogvmhqv`. This is the authoritative sync mechanism for this repo,
  and it confirms remote schema matched local migrations as of that merge.
- **This session had no live Supabase CLI/MCP connection**, so remote state
  could not be independently re-queried — the above is inferred from CI logs,
  not a fresh `db:status`/`db:verify` run. Treat as high-confidence, not
  independently re-verified.
- `STAGING_AUDIT.md` (dated 2026-06-18, last re-verified 2026-06-22, frontier
  0056) and the "next session" section of `OLUNE_PROGRESS.md` are both far out
  of date and should not be trusted for current migration state.

### Vercel — deploys succeeding, direct verification blocked
- GitHub commit statuses confirm Vercel deployed both open PR previews
  successfully (`Deployment has completed`, project `olune/nzad`).
- **This session's Vercel MCP connection has zero linked projects** under the
  reachable "Olune" team (hobby plan) — `list_projects` and
  `get_git_deployment_context` both return empty, even though the GitHub → Vercel
  integration is clearly active. This means production deployment status for
  `main`'s tip (`45ab14f`) **could not be directly confirmed** this session.
  Likely cause: the Vercel account/token behind this MCP connector isn't the
  one that owns the `nzad` project. Reconnect the correct account before the
  next audit if deploy verification matters.

**Bottom line:** GitHub and Supabase are verifiably aligned as of the last
merge. Vercel is presumed healthy (PRs deploy fine) but not independently
confirmed for production this session. Two feature PRs are ready-reviewed but
not merged — see Priority Queue.

---

## 2. Completed Work (2026-09-01)

- **Expo / parent app**: made the Expo app resolve against a real SDK
  (`859ed70`); shipped parents an app with a third channel (email) for Olune
  to reach them (`5775aef`).
- **Studio audit trail**: gave studios their own audit trail (`b36d2b6`),
  landing as migration `0126_audit_events.sql` in the SOC2 merge.
- **Email deliverability**: stopped seeded demo parents from burning the new
  sending domain (`b630ed4`); made Olune actually send email and made silence
  audible — i.e. failures now surface instead of failing silently (`a8565ac`);
  replied to whoever actually sent the mail (`cba9b88`).
- **SOC2 remediation** (merged via PR #48, `dfa8754`):
  - Closed a recurring SECURITY DEFINER hole (`9e34004`).
  - Made the Google sign-in purge actually purge (`c74774d`).
  - Indexed every foreign key ahead of delete-heavy workloads (`c3b3d3c`).
  - Stopped CI from seeding a production superuser on every merge.
  - Added the `audit_events` table.
  - Migrations `0120`–`0126` (push device tokens, security-linter fixes, RLS
    initplan round 2, scope policies to `authenticated`, stock-oversell guard,
    FK indexes, audit events).
- **Copy/UX fixes**: NZ$ (not NZD) on the price caption (`b928d80`); stopped
  implying loyalty perks are Olune's to set rather than the studio's
  (`7ad904d`); deleted perks the card page was promising on studios' behalf
  (`d239ccd`).
- **Loyalty ladder redesign** (merged via PR #49, `c6c5236`): Bronze now lands
  on day one instead of after 3 years; Silver/Gold/Platinum/Diamond retimed to
  1/3/5/10 years; empty rewards panel now seeds four "ideas, not offers" per
  tier for studios to price and fund themselves.
- **In flight, not yet merged**: Sentry error monitoring + shared Redis rate
  limiter (PR #51, draft); `SECURITY.md` policy (PR #50).

---

## 3. Missing / Incomplete Items

Gap analysis against `MARKET_READINESS.md` (2026-08-06 audit) and the two open
PRs' own "still open" notes:

| Item | Status |
|---|---|
| §1.1 Error monitoring (Sentry) | **Built, not merged** — PR #51, draft |
| §1.2 Rate limiting (shared store) | **Built, not merged** — PR #51, draft |
| §1.3 Cron cadence too slow | **Still open.** PR #51's own description flags that `cron/deliver-notifications` documents itself as "every 5 minutes" while `vercel.json` runs it once daily at 10:00 UTC — confirmed still true in the current `vercel.json`. Nobody has fixed the mismatch yet (either the comment or the schedule is wrong). |
| §1.4 Uptime/status monitoring | Not started |
| §1.5 Playwright E2E coverage | Not started — still only pure-logic unit tests |
| §2.1 Terms of Service | Not started ("that one's writing, not code" — PR #51) |
| §2.2 GDPR export, retention policy, sub-processor list, COPPA | Not started |
| §2.3 Audit logging | **Partial progress** — `audit_events` table shipped yesterday, but scope/coverage against the audit's "every privileged cross-tenant action, every refund, every role change, every export" bar hasn't been re-assessed |
| §2.4 Backup/restore runbook | Not documented |
| §0.3 Documentation staleness | **Still true, worse than before.** `OLUNE_PROGRESS.md`'s "NEXT SESSION — Start here" section is frozen at Session 26 (2026-06-22, migration frontier "0056") while the real frontier is now 0126. `STAGING_AUDIT.md` is dated 2026-06-18. Neither reflects current reality. |
| Process gap | No `DAILY_REPORT.md` / `NEXT_STEPS_HANDOFF.md` exists in the repo for this audit to compare against, despite the mission expecting one. This file fills that role going forward. |
| Housekeeping | 2 fully-merged branches (`soc2-merge`, `claude/soc2-compliance-exam-nd6akr`) left undeleted |
| PR #50 blocker | `security@olune.co.nz` deliverability unverified — needs an actual test email before merge |

---

## 4. Today's Priority Queue

1. **Take PR #51 out of draft and merge it.** It closes the audit's two
   highest-leverage gaps (error monitoring, real rate limiting) and is already
   green on CI with a working preview deploy. This is the single highest-value
   action available right now.
2. **Verify `security@olune.co.nz` receives mail**, then merge PR #50 (or swap
   the contact address to `support@olune.co.nz` and update the PR).
3. **Fix the `deliver-notifications` cadence mismatch** (§1.3): decide whether
   the cron should actually run sub-daily (Vercel Pro / QStash / `pg_cron`) or
   the "every 5 minutes" comment is simply wrong, and make the code and the
   config agree.
4. **Delete the two stale merged branches** (`soc2-merge`,
   `claude/soc2-compliance-exam-nd6akr`) once a human confirms nothing depends
   on them.
5. **Refresh `OLUNE_PROGRESS.md`'s handoff section and `STAGING_AUDIT.md`** —
   both describe a codebase roughly 70 migrations behind current `main` and
   will actively mislead the next session (including this kind of automated
   one) if left as-is.
6. **Reconnect the Vercel MCP integration to the account that owns the `nzad`
   project** so future audits can confirm production deployment status
   directly instead of inferring it from PR preview checks.
7. Continue down the `MARKET_READINESS.md` list in order: uptime/status
   monitoring (§1.4), Playwright E2E on the four money-path flows (§1.5), then
   start the Terms of Service draft (§2.1) since it blocks onboarding paying
   studios.
