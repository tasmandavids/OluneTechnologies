/** Flat price for a single adult ballet class pass — cents, studio's CURRENCY (NZD). */
export const CLASS_PASS_PRICE_CENTS = 2500;

/** Xero chart-of-accounts code class-pass invoice line items are classified under. */
export const CLASS_PASS_XERO_ACCOUNT_CODE = "200-01";
