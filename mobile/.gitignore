node_modules/
.expo/
dist/
web-build/
*.log

# Native projects are generated by prebuild — never commit them, or the config
# in app.config.ts silently stops being the source of truth.
/ios
/android

# Secrets and signing material
.env
.env.local
# The root .gitignore excludes `.env*`, which would swallow the template the
# README tells you to copy. It is placeholders only.
!.env.example
*.p8
*.p12
*.mobileprovision
*.keystore
play-service-account.json
